# Fetch API Demo

This project demonstrates a simple frontend that fetches data from an external API using JavaScript (fetch).

---

### 🔧 How it works:

1. User clicks the **Fetch Data** button.
2. The frontend sends a request to the backend mock API.
3. The backend returns JSON data.
4. The UI updates and displays the response on the screen.

---

### 📁 Project Files:

- `index.html` — UI structure
- `script.js` — fetch logic
- `style.css` (optional)

---

### 🚀 API Used:
https://jsonplaceholder.typicode.com/posts/1

Returns data in JSON format.

---

### Screenshot

(Located in `/screenshots/` folder)
